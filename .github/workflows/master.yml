name: master-env

on:
  push:
    tags:
      - "[0-9][0-9][0-9][0-9].[0-9]+"

jobs:
  build:
    runs-on: self-hosted

    steps:
      - name: branch checking
        uses: actions/setup-node@v3

      #- run: npm i
      #- run: npm run build
      - run: echo "deploying master"
